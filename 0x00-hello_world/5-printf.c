#include <stdio.h>

/**
  * main - Entry point of the program
  * takes no args
  *
  * Description: used printf(); function to print to stdout
  * Return: Program always return 0(success)
  */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
